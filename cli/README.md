# cli/

The original `morning` wake-up CLI (Python 3, stdlib only). Shipped and working.

Run: `python3 cli/morning.py` (your `morning` shell alias points here).

Its streak / hit-detection / anti-snooze logic is the seed for the backend in Phase 2 —
keep it as the reference implementation of the rules.
